version: '3.9'

services:
  web:
    build: .
    container_name: food_delivery_app
    command: gunicorn foodproject.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"